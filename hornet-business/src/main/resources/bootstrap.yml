spring:
  application:
    name: hornet-business
  profiles:
    active: dev

---
# 开发环境配置
spring:
  profiles: dev
logging:
  file: log/hornet-business.log

---
# 测试环境配置
spring:
  profiles: test
logging:
  file:

---
# 产品环境配置
spring:
  profiles: online
